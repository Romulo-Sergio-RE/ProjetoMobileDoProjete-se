import React from 'react';
import { StyleSheet } from 'react-native';

export default StyledBuscarProjetos = StyleSheet.create({
    container:{
        flexDirection:'column',
        alignItems:'center',
        justifyContent:'center', 
             
    },
    containerTitulo:{
        marginTop: 15,
        marginRight: 40,
    },
    
});